# Codex 开发规则

本文件是 English Pet Care Plan 的项目级 Codex 指令。规则保持精简、可执行；如与用户当次明确要求冲突，以用户要求为准。

## 1. 功能开发流程

每项功能开始前，先阅读 `README.md`、`TODO.md`、`CHANGELOG.md` 和相关代码，并明确：

1. 用户价值、范围、非目标和验收标准。
2. 数据来源、所有权、权限边界、失败路径和回滚方式。
3. 需要新增或更新的 UnitTest、UITest、文档和变更记录。
4. 影响的性能关键路径及可测量指标。

优先完成最小、可验证且可回退的改动；不要把无关重构、格式化或依赖升级混入功能提交。需求、规则或安全边界不明确时，先询问，不自行扩大范围。

## 2. 安全与数据边界

- 技术基线为 **Taro + React + TypeScript** 小程序、**FastAPI + Python** 服务端、PostgreSQL + SQLAlchemy 数据库；详细结构见 `docs/architecture.md`。后续移动端迁移优先复用 API、类型和设计令牌，平台 UI 与平台能力通过适配层实现。
- 客户端只负责展示和发起请求，**不得**作为积分、库存、健康、疾病、死亡、排行榜或权限的最终事实来源。
- FastAPI 服务端是业务规则唯一权威；以其生成的 OpenAPI 作为接口契约。前端不得复制积分数值、状态机或服务端时间判定逻辑。
- 所有影响余额、库存、宠物状态和排名的写操作必须在服务端完成身份验证、资源归属校验和状态转换校验。
- 积分与库存使用不可变账本；兑换、领奖、照料、治疗和定时结算必须具备幂等键，并以事务保证原子性。
- 宠物照料期限、疾病窗口和死亡时间以服务端时间为准。定时任务必须可重试、可重复执行且不会重复扣减或结算。
- 所有外部输入（表单、URL、文件、语音转写、第三方回调）在信任边界处进行模式校验、长度限制和输出编码；数据库访问必须参数化。
- 采用最小权限：每个查询和写入都按当前用户过滤；管理操作、跨用户读取和批量修正须单独授权并留下审计记录。
- 不提交密钥、令牌、真实用户数据或生产导出文件；使用环境变量和密钥管理服务，日志中不得输出凭据或敏感内容。
- 引入依赖、第三方服务、文件上传或公开接口时，记录数据流、权限、速率限制、超时和降级策略；修复高风险漏洞前不得发布。
- 错误信息对用户可理解但不暴露堆栈、查询、密钥或内部实现；服务端记录带关联 ID 的脱敏诊断信息。

## 3. 性能与可靠性

- 先测量后优化；每个影响核心流程的 PR 说明性能关键路径、查询次数和验证结果。
- 列表查询必须分页/限量，避免全表读取；避免 N+1 查询，并为常用筛选、排序和关联建立合适索引。
- 网络请求必须设置超时、取消/去重策略和明确的加载、空状态、重试与错误状态；不可用时不得丢失已确认的学习或照料操作。
- 对高频操作实施服务端速率限制和幂等处理；对慢任务使用异步队列或后台任务，并提供可观察的处理状态。
- 关键状态变更需要审计事件；监控错误率、延迟、任务失败率和异常积分/库存行为，并为告警提供责任边界。

## 4. 测试要求

### UnitTest

- 小程序前端使用 **Vitest + Testing Library**；Python 服务端使用 **Pytest**。领域测试靠近服务端领域代码，前端测试只验证展示、交互、请求状态和平台适配。
- 新增或修改的领域规则必须有 UnitTest，至少覆盖正常路径、边界值、拒绝路径和重复请求。
- 积分账本、库存事务、照料状态机、疾病/死亡阈值、周榜计算、时区和幂等逻辑属于 P0：没有测试不得合并。
- 时间相关测试使用可控制的时钟；随机逻辑使用可注入种子；测试不得依赖真实网络、真实时间或执行顺序。
- 修复缺陷时先补充能复现问题的回归测试；确实无法测试时，在 PR 中说明原因与替代验证证据。

### UITest

- 每个 P0 用户流程至少有一条端到端 UITest：登录/会话、完成学习、获得积分、购买资源、完成照料、状态变化和异常提示。
- H5 冒烟与回归流程使用 **Playwright**；微信登录、授权、支付（如后续引入）和小程序平台 API 使用微信开发者工具/真机自动化补充验证。后续原生 App 按所选运行时配置等价 UITest。
- 关键失败流程同样需要验证：权限不足、余额不足、网络失败、重复提交、任务超时和资源过期。
- 优先使用用户可见的语义选择器；仅在必要时使用稳定的 `data-testid`，不得依赖样式类、像素坐标或易变文本。
- 测试数据必须隔离并在结束后清理；测试环境不得连接生产数据或生产密钥。

### 测试执行

- M0 必须落地单元测试运行器、浏览器 UITest 运行器和 CI 命令。当前统一命令为 `make web-check`、`make api-check`、`make api-test` 和 `npm run test:ui`；CI 同时执行前端与后端检查。
- 每次改动至少运行受影响的测试、类型检查、格式检查和构建检查；合并前 CI 必须通过全部必需检查。
- 不以覆盖率数字替代质量判断；核心规则和关键流程必须有可读、独立、稳定的测试案例。

## 5. 代码与接口质量

- 保持模块职责单一，使用清晰命名和显式类型；不要以隐式全局状态、魔法数字或吞错替代设计。
- 所有 API 定义输入、输出、错误码和授权要求；接口变更须兼顾迁移、兼容性或明确的弃用计划。
- Python API 使用 Pydantic 模型明确输入与输出；接口变更先更新 OpenAPI 契约和客户端类型，再实现调用方。服务端配置经环境变量读取，客户端构建变量不得包含秘密。
- 数据库迁移必须可在空库和升级路径执行；破坏性迁移须先给出备份、回滚和数据校验方案。
- 用户界面必须覆盖加载、空、错误、成功和禁用状态，支持键盘操作、可访问名称和足够的颜色对比度。

## 6. 提交、文档与完成定义

- 每项面向用户的改动更新 `CHANGELOG.md`；计划进度变化更新 `TODO.md`；规则变化更新 `README.md` 或本文件。
- 提交聚焦单一目的，提交信息清楚说明改动；暂存时只添加已确认路径，禁止使用 `git add .`、`git add -A` 或 `git add --all`。
- PR 需写明变更摘要、规则/数值影响、验证方式和已知限制；已有匹配 PR 时更新它，不重复创建。
- 功能只有在实现、授权校验、错误处理、UnitTest、UITest（适用时）、文档和必要监控均完成后才算完成。

## 7. 操作授权边界

- 可直接进行：阅读项目文件、编辑当前任务范围内的代码、运行非破坏性检查和测试。
- 必须先确认：外部写入（推送、创建 PR、发布、发送消息）、删除或覆盖重要数据、改变权限/密钥、购买或引入高风险依赖，以及实质扩大任务范围。

## 8. 多 Agent 协作

当用户明确要求多 Agent 协作时，采用“主 Agent 统筹、子 Agent 专业分工、主 Agent 集成验收”的模式。子 Agent 只处理被分配的、有明确输入和交付物的任务；主 Agent 保持对范围、安全、依赖顺序、冲突处理、最终修改与外部操作的责任。

### 8.1 角色与文件边界

| 角色                      | 主要职责                                                                 | 默认可写范围                                                                                                                 | 必须交付                                              |
| ------------------------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| 主 Agent / 项目经理       | 拆分任务、决定顺序、冻结接口、整合改动、运行全量验证、处理冲突和对外沟通 | 全项目，但只在集成阶段修改跨域文件                                                                                           | 任务卡、集成结果、验证证据、风险与下一步              |
| 子 Agent 1 / 产品经理     | 需求分析、需求评审、原型范围、验收标准、产品实验设计                     | `README.md`、`TODO.md`、`CHANGELOG.md`、`docs/product-requirements.md`、`docs/prototype/`；涉及范围变更时须先由主 Agent 确认 | 需求评审记录、优先级、非目标、验收标准                |
| 子 Agent 2 / UI/UX 设计师 | 信息架构、交互、状态、无障碍、视觉与性能预算                             | `docs/ui-ux/` 和获主 Agent 指定的原型文件；不得修改业务规则或后端契约                                                        | 可交付给前端的页面/状态/交互说明和验收清单            |
| 子 Agent 3 / 后端开发     | 数据模型、迁移、FastAPI、授权、事务、幂等、定时结算和 Pytest             | `services/api/`、相关 API 契约文档；不得修改前端页面                                                                         | OpenAPI 变更、迁移/回滚说明、单元测试与验证结果       |
| 子 Agent 4 / 前端开发     | Taro 页面、状态展示、请求层、可访问性、Vitest/Playwright                 | `apps/mini/`；只消费已冻结的 OpenAPI/类型，不复制服务端规则                                                                  | 页面实现、前端测试、构建与 UI 验证结果                |
| 子 Agent 5 / 测试         | 按验收标准验证、补充测试建议、复现并报告缺陷、回归确认                   | 默认只读；可在主 Agent 明确授权后仅写测试文件                                                                                | 缺陷报告（复现、预期/实际、严重级别、证据）、回归结论 |

测试 Agent 不直接修改产品或业务实现；缺陷交回责任开发 Agent 修复后，由测试 Agent 复测。

### 8.2 协作节奏与质量门

1. 主 Agent 创建任务卡：用户价值、范围/非目标、负责人、允许改动的路径、接口依赖、验收标准和验证命令。
2. 产品经理先完成需求评审；涉及规则、数值、数据收集或商业化的调整必须写入需求基线后，才能进入实现。
3. UI/UX 与后端可并行产出设计和 API 草案。主 Agent 审核并冻结 OpenAPI/状态机/错误码后，前端才开始对接。
4. 前端和后端分别运行受影响的 UnitTest、类型/格式检查与构建；跨端流程由测试 Agent 运行 UITest 和人工验证。
5. 测试 Agent 按 P0/P1/P2 报告问题。P0 未关闭不得合并；修复必须附回归测试或替代验证证据。
6. 主 Agent 只在所有必需检查通过、文档同步、冲突解决并完成安全/性能审查后，才将任务标记为完成或请求外部写入授权。

### 8.3 并发与安全规则

- 并发只用于独立、边界清晰的工作；存在顺序依赖、共享同一可写文件或需要频繁同步的任务，按阶段串行执行。
- 同一时刻同一文件只能有一个写入责任人；跨域文件、`AGENTS.md`、`README.md`、`TODO.md`、`CHANGELOG.md`、OpenAPI 契约和数据库迁移由主 Agent 集成，避免覆盖他人工作。
- 子 Agent 开始前必须读取本文件、当前需求基线和其负责代码；结束时报告已读输入、改动路径、运行的检查、风险和未完成项。
- 不把密钥、生产数据、外部写入权限或不可逆操作委托给子 Agent。推送、创建 PR、发布和真实付款始终由主 Agent 按用户授权执行。
- 当前运行环境的并发配额不足以同时启用所有角色时，主 Agent 按“产品/设计/后端 → 前端 → 测试”的波次调度，并保留同一任务的上下文与审计记录。
